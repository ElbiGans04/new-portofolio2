import { Heading, Highlight, Link, Text, VStack } from "@chakra-ui/react";
import Head from "next/head";
import NextLink from "next/link";
import urls from "@/src/constants/url";

export default function NotFound({}) {
  return (
    <>
      <Head>
        <title>Page Not Found</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <VStack
        w={["100%"]}
        height={["100%"]}
        spacing={["50px", "20px"]}
        paddingX={[0, null, null, null, "48px", "0"]}
        paddingY={["50px"]}
        alignItems={"flex-start"}
      >
        <Heading
          as="h1"
          color="brand.50"
          fontSize={["28px", "32px", "48px", "56px", "64px"]}
        >
          Page Not Found
        </Heading>
        <Text
          fontSize={["18px", "22px", "24px", "28px"]}
          textAlign={["left"]}
        >
          <Highlight
            query={["wrong", "changed", "deleted."]}
            styles={{ fontWeight: "bold", color: "brand.50" }}
          >
            The link you are pointing to may be wrong or it may be your link
            search may have changed or been deleted.
          </Highlight>
        </Text>
        <Link
          fontSize={["18px", "22px", "24px", "28px"]}
          textAlign={["left"]}
          fontWeight={["bold"]}
          color={["brand.50"]}
          as={NextLink}
          href={urls.blogs.url}
        >
          Don{"'"}t worry, you can read the blog post I wrote! Just click on this text {':)'}
        </Link>
      </VStack>
    </>
  );
}
